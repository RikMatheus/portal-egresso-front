import styled from "styled-components"
import { theme } from "../../theme"

export const StyledCourses = styled.aside`
    width: 100%;
    height: 100vh;
    padding: 2rem;
    position: absolute;
    top: 0;
    background-color: ${theme.colors.secondary};
`